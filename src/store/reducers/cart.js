

const initialState = {
    items: {},
    totalPrice: null,
    totalAmount: null
}


const Cart = (state = initialState , action) => {

     switch(action.type) {
         case 'ADD_APARTMENT_CART': {
             return 

     }
     default: 
     return state

     }
}

export default Cart